<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RecipePOC.AdvancedSearchListview" 
                          Title="AdvancedSearch"
                          Shell.BackgroundColor="#F9EDEC"    
             Shell.TitleColor="#000000"
             xmlns:local="clr-namespace:RecipePOC"
             Shell.ForegroundColor="#000000"
             BackgroundColor="WhiteSmoke">
    <Shell.TitleView>
        <Grid ColumnDefinitions="*,Auto"
             Padding="12,0"
             HeightRequest="56">

            <Label Text="Search Results"
                  FontSize="25"
                  TextColor="#E57373"
                  FontAttributes="Bold"
                  VerticalOptions="Center" />
        </Grid>
    </Shell.TitleView>
    <Grid RowDefinitions="*">
        <ListView x:Name="SearchListview"
        ItemsSource="{Binding Items}"
        HasUnevenRows="True"
                BackgroundColor="White"
        SeparatorVisibility="None">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Spacing="0">

                            <StackLayout.GestureRecognizers>
                                <TapGestureRecognizer
                Command="{Binding Source={RelativeSource AncestorType={x:Type ListView}}, Path=BindingContext.ToggleExpandCommand}"
                CommandParameter="{Binding .}" />
                            </StackLayout.GestureRecognizers>
                            <!-- MAIN ROW -->
                            <Grid ColumnDefinitions="60,*,30"
                          Padding="10">

                                <!-- 🖼 PHOTO -->
                                <Image Grid.Column="0"
                               Source="{Binding RecipePhoto}"
                               WidthRequest="50"
                               HeightRequest="50"
                               Aspect="AspectFill"
                               BackgroundColor="#eee" />

                                <!-- 📝 TITLE + SHORT DESC -->
                                <StackLayout Grid.Column="1"
                                     Spacing="2">
                                    <Label Text="{Binding Title}"
                                           TextColor="Black"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   LineBreakMode="TailTruncation" />

                                    <Label Text="{Binding Description}"
                                   FontSize="13"
                                   LineBreakMode="TailTruncation"
                                   MaxLines="2"
                                   TextColor="#E57373" /> 
                                </StackLayout>

                                <!-- ▶ / ▼ CHEVRON -->
                                <Label Grid.Column="2"
                               Text="▶"
                                       TextColor="Black"
                               FontSize="18"
                               VerticalOptions="Center"
                               HorizontalOptions="End">
                                    <Label.Triggers>
                                        <DataTrigger TargetType="Label"
                                             Binding="{Binding IsExpanded}"
                                             Value="True">
                                            <Setter Property="Text" Value="▼" />
                                        </DataTrigger>
                                    </Label.Triggers>
                                </Label>
                            </Grid>

                            <!-- EXPANDED DETAILS -->
                            <StackLayout Padding="10"
                                 IsVisible="{Binding IsExpanded}"
                                 BackgroundColor="#f7f7f7"
                                 Spacing="4">

                                <Label Text="{Binding Description}" TextColor="Black" />

                                <Label Text="{Binding Prep, StringFormat='Prep: {0} min'}"                                        TextColor="Black"
/>
                                <Label Text="{Binding CookTime, StringFormat='Cook: {0} min'}"                                       TextColor="Black"
 />
                                <Label Text="{Binding Serves, StringFormat='Serves: {0}'}"                                       TextColor="Black"
 />
                                <Label Text="{Binding Stars, StringFormat='⭐ {0}'}"                                        TextColor="Black"
/>

                            </StackLayout>

                        </StackLayout> 
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid> <!-- vertical stack layout -->
</ContentPage>