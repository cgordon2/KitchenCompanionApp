<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RecipePOC.Notifications"
             Title="Notifications"
                          Shell.BackgroundColor="#F9EDEC"    
            BackgroundColor="WhiteSmoke"
             Shell.ForegroundColor="#2d2d2d" >
    <Shell.TitleView
    Shell.BackButtonBehavior="{x:Static Shell.BackButtonBehavior.Default}">
        <Grid ColumnDefinitions="*,Auto"
          Padding="12,0"
          HeightRequest="56">

            <!-- Page title -->
            <Label Text="5 Unread Alerts"
               FontSize="25"
               TextColor="#E57373"
               FontAttributes="Bold"
               VerticalOptions="Center"
               HorizontalOptions="Start" />

            <!-- Right-aligned Edit button -->
            <Button Grid.Column="1"
                Text="✔ Read"
                FontSize="20"
                    Clicked="MarkRead_Clicked"
                TextColor="#E57373"      
                BackgroundColor="Transparent"
                FontAttributes="Bold"
                HorizontalOptions="End"
                VerticalOptions="Center"
                Padding="0"  />
        </Grid>
    </Shell.TitleView>
    <Grid RowDefinitions="Auto,*,Auto"> 

        <ListView Grid.Row="1"
          x:Name="RecipesList2"
          ItemsSource="{Binding Recipes}"
          SeparatorVisibility="Default"
          HasUnevenRows="True"
          BackgroundColor="WhiteSmoke">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal" Padding="10" Spacing="10">
                            <CheckBox VerticalOptions="Center" Color="#E57373" IsChecked="{Binding IsSelectedForRead}"/>
                            <BoxView WidthRequest="5"
                             VerticalOptions="FillAndExpand"
                             Color="{Binding StripColor}" />
                            <StackLayout Orientation="Vertical" Spacing="2" VerticalOptions="CenterAndExpand">
                                <Label Text="{Binding Message}"
                               FontSize="18"
                               FontAttributes="Bold"
                               TextColor="#2d2d2d" /> 
                            </StackLayout>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


        <BoxView Grid.Row="2"
             HeightRequest="1"
             Color="#ddd"/>
    </Grid>

</ContentPage>
