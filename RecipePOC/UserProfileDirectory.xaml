<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RecipePOC.UserProfileDirectory"
                          Shell.BackgroundColor="#F9EDEC"    
             Shell.TitleColor="#000000"
             xmlns:local="clr-namespace:RecipePOC"
             Shell.ForegroundColor="#000000"
             BackgroundColor="WhiteSmoke"
             Title="UserProfileDirectory">
    <Shell.TitleView>
        <Grid ColumnDefinitions="*,Auto"
              Padding="12,0"
              HeightRequest="56">

            <Label Text=""
                   FontSize="25"
                   TextColor="#E57373"
                   FontAttributes="Bold"
                   VerticalOptions="Center" /> 
        </Grid>
    </Shell.TitleView>

    <ScrollView>
        <VerticalStackLayout Padding="20"
                                 HorizontalOptions="Center"
                                 VerticalOptions="Start"
                                 Spacing="20">
            <Border BackgroundColor="#F9EDEC"
                        StrokeShape="RoundRectangle 20"
                        StrokeThickness="0"
                        Padding="20"
                        WidthRequest="1000"
                        MaximumWidthRequest="600"
                        HorizontalOptions="Center">

                <VerticalStackLayout Spacing="15"
                                         HorizontalOptions="Center"
                                         VerticalOptions="Center">
                    <Border WidthRequest="110"
                                HeightRequest="110"
                                StrokeThickness="0"
                                BackgroundColor="#F9EDEC"
                                HorizontalOptions="Center"
                                VerticalOptions="Center"
                                Padding="0">
                        <Image Source="{Binding UserAvatar}"
                                   Aspect="AspectFill"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center"
                                   Margin="1"
                                   WidthRequest="108"
                                   HeightRequest="108">
                            <Image.Clip>
                                <EllipseGeometry Center="54,54" RadiusX="54" RadiusY="54" />
                            </Image.Clip>
                        </Image>
                    </Border>

                    <Label Text="{Binding RealName}"
                               FontAttributes="Bold"
                               FontSize="22"
                               HorizontalOptions="Center"
                               TextColor="#000" />

                    <Label Text="{Binding UserName}"
                               FontSize="14"
                               HorizontalOptions="Center"
                               TextColor="#555" />

                    <Label Text="{Binding ShortBio}"
                               FontSize="14"
                               TextColor="#333"
                               LineBreakMode="WordWrap"
                               HorizontalTextAlignment="Center"
                               Margin="0,4,0,0" />
                    <HorizontalStackLayout Spacing="12"
                       HorizontalOptions="Center"
                       Margin="0,8,0,0">
                        <HorizontalStackLayout Spacing="4">
                            <Label Text="📍" FontSize="14" />
                            <Label Text="{Binding Location}"
               FontSize="14"
               TextColor="#555" />
                        </HorizontalStackLayout>
                        <HorizontalStackLayout Spacing="4">
                            <Label Text="📅" FontSize="14" />
                            <Label Text="{Binding CreationDate}"
               FontSize="14"
               TextColor="#555" />
                        </HorizontalStackLayout>
                    </HorizontalStackLayout>
                    <BoxView HeightRequest="1"
                                 BackgroundColor="#E0E0E0"
                                 HorizontalOptions="Fill"
                                 Margin="0,10,0,0"/>
                    <HorizontalStackLayout Spacing="20"
                            HorizontalOptions="Center"
                            Margin="0,10,0,0">
                        <VerticalStackLayout HorizontalOptions="Center"
                                        VerticalOptions="Center">
                            <Label Text="{Binding TotalRecipes}"
                                       FontAttributes="Bold"
                                       FontSize="14"
                                       TextColor="#000"
                                       HorizontalOptions="Center"/>
                        </VerticalStackLayout>

                        <!-- Divider -->
                        <!--<BoxView WidthRequest="1"
             HeightRequest="20"
             BackgroundColor="#E0E0E0"
             VerticalOptions="Center"/>-->

                        <!-- Followers -->
                        <!--<VerticalStackLayout HorizontalOptions="Center"
                         VerticalOptions="Center">
                            <Label Text="{Binding TotalFollowers}"
               FontAttributes="Bold"
               FontSize="14"
               TextColor="#000"
               HorizontalOptions="Center"/>
                        </VerticalStackLayout> 
                        <BoxView WidthRequest="1"
             HeightRequest="20"
             BackgroundColor="#E0E0E0"
             VerticalOptions="Center"/> 
                        <VerticalStackLayout HorizontalOptions="Center"
                         VerticalOptions="Center">
                            <Label Text="{Binding TotalFollowing}"
               FontAttributes="Bold"
               FontSize="14"
               TextColor="#000"
               HorizontalOptions="Center"/>
                        </VerticalStackLayout>-->
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>
            <ListView x:Name="RecipesList"
          Grid.Row="1"
          Margin="10,10,10,10"
          SeparatorVisibility="None"
          HasUnevenRows="True" 
          ItemsSource="{Binding Recipes}"> 
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid ColumnDefinitions="5,*"
                          Padding="0"
                          x:Name="RowGrid">

                                <!-- Left bar -->
                                <BoxView x:Name="LeftBar"
                                 Grid.Column="0"
                                 WidthRequest="5"
                                 HorizontalOptions="Fill"
                                 VerticalOptions="Fill" /> 
                                <Border Grid.Column="1" 
                                Padding="10"
                                Margin="5,5,10,5"
                               
                                BackgroundColor="#FFF9F9"
                                StrokeThickness="0"
                                > 
                                    <!--<HorizontalStackLayout Spacing="10" VerticalOptions="Center"> 
                                        <Image Source="{Binding Photo}"  
                                            WidthRequest="60"
                                       HeightRequest="60"
                                       Aspect="AspectFill"
                                       HorizontalOptions="Start"
                                       VerticalOptions="Center" 
                                       BackgroundColor="#f0f0f0" /> 
                                        <VerticalStackLayout Spacing="4"
                                                     VerticalOptions="Center" >
                                            <Label Text="{Binding Title}"
                                           FontAttributes="Bold"
                                           FontSize="18"
                                                                      FontFamily="OpenSansRegular"

                                           TextColor="#2d2d2d"
                                           LineBreakMode="TailTruncation" />
                                            <Label Text="{Binding Description}"
                                           FontSize="14"
                                           TextColor="#666"
                                           LineBreakMode="WordWrap" /> 
                                            <HorizontalStackLayout Spacing="4"
                                                           VerticalOptions="Center">
                                                <Border StrokeShape="RoundRectangle 2"
                                                HeightRequest="15"
                                                WidthRequest="15"
                                                BackgroundColor="Transparent"
                                                Padding="0"
                                                HorizontalOptions="Center"
                                                VerticalOptions="Center">
                                                    <Image Source="user_ico.png"
                                                   Aspect="AspectFill"
                                                   BackgroundColor="Transparent"/>
                                                </Border>
                                                <Label Text="{Binding UserName}"
                                               FontSize="12"
                                               TextColor="#999"
                                               VerticalOptions="Center"/>
                                            </HorizontalStackLayout>
                                        </VerticalStackLayout>
                                    </HorizontalStackLayout>-->
                                    <Grid ColumnDefinitions="Auto,*,Auto"
      VerticalOptions="Center"
      ColumnSpacing="10">

                                        <!-- 🖼️ Image -->
                                        <Image Grid.Column="0"
           Source="{Binding RecipePhoto}"
           WidthRequest="60"
           HeightRequest="60"
           Aspect="AspectFill"
           VerticalOptions="Center"
           BackgroundColor="#f0f0f0" />
                                        <VerticalStackLayout Grid.Column="1"
                     Spacing="6"
                     VerticalOptions="Center">

                                            <!-- HEADER ROW -->
                                            <Grid ColumnDefinitions="*,Auto">

                                                <!-- Text -->
                                                <VerticalStackLayout Grid.Column="0" Spacing="4">
                                                    <Label Text="{Binding Title}"
                   FontAttributes="Bold"
                   FontSize="18"
                   TextColor="#2d2d2d"
                   LineBreakMode="TailTruncation" />
 
                                                </VerticalStackLayout>

                                                <!-- CHEVRON -->
                                                <Label Grid.Column="1"
               Text="▶"
               TextColor="Black"
               FontSize="18"
                                                       InputTransparent="False"
               VerticalOptions="Center"
               HorizontalOptions="End">

                                                    <Label.Triggers>
                                                        <DataTrigger TargetType="Label"
                             Binding="{Binding IsExpanded}"
                             Value="True">
                                                            <Setter Property="Text" Value="▼" />
                                                        </DataTrigger>
                                                    </Label.Triggers>

                                                    <Label.GestureRecognizers>
                                                        <TapGestureRecognizer
        Command="{Binding Source={RelativeSource AncestorType={x:Type ListView}},
                          Path=BindingContext.ToggleExpandCommand}"
        CommandParameter="{Binding .}" />
                                                    </Label.GestureRecognizers>
                                                </Label>
                                            </Grid>

                                            <!-- EXPANDABLE CONTENT -->
                                            <VerticalStackLayout IsVisible="{Binding IsExpanded}"
                         Padding="8"
                         BackgroundColor="#F0F0F0"
                         Spacing="4">

                                                <Label Text="{Binding ExtraInfo}"
               FontSize="14"
               TextColor="#444" />

                                                <!-- add more expanded-only content here -->
                                            </VerticalStackLayout>

                                        </VerticalStackLayout>
                                        <!-- 📜 Text -->
                                        <!--<VerticalStackLayout Grid.Column="1"
                         Spacing="4"
                         VerticalOptions="Center">

                                            <Label Text="{Binding Title}"
               FontAttributes="Bold"
               FontSize="18"
               TextColor="#2d2d2d"
               LineBreakMode="TailTruncation" />

                                            <Label Text="{Binding Description}"
               FontSize="14"
               TextColor="#666"
               LineBreakMode="WordWrap" />
                                            <Label Text="▶"
           TextColor="Black"
           FontSize="18"
           HorizontalOptions="End">

                                                <Label.Triggers>
                                                    <DataTrigger TargetType="Label"
                         Binding="{Binding IsExpanded}"
                         Value="True">
                                                        <Setter Property="Text" Value="▼" />
                                                    </DataTrigger>
                                                </Label.Triggers>

                                                <Label.GestureRecognizers>
                                                    <TapGestureRecognizer
                Command="{Binding BindingContext.ToggleExpandCommand,
                                  Source={x:Reference UserProfileDirectory}}"
                CommandParameter="{Binding .}" />
                                                </Label.GestureRecognizers>
                                            </Label>

                                        </VerticalStackLayout>-->

                                        <!--<Button Grid.Column="2"
        Text="＋"
        FontSize="20"
        FontAttributes="Bold"
        WidthRequest="32"
        HeightRequest="32"
        Padding="0"
        BackgroundColor="#E57373"
        TextColor="White"
        CornerRadius="16"
        VerticalOptions="Center"
        HorizontalOptions="End"
                   Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}},
                          Path=BindingContext.CloneCommand}"
        CommandParameter="{Binding .}"/>-->

                                    </Grid>

                                </Border>
                            </Grid>
                            <!--<Frame HasShadow="False"
                               CornerRadius="0"
                       BackgroundColor="#FFF9F9"
                       Margin="0,5"
                       Padding="0">

                        </Frame>-->
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <!-- <ListView.Footer>
                <StackLayout Padding="10"
                     VerticalOptions="Start">
                    <Button Text="Load More"
                    BackgroundColor="Black"
                    CornerRadius="10"
                    TextColor="White"
                    WidthRequest="150"
                    HorizontalOptions="Center" />
                </StackLayout>
            </ListView.Footer>-->
            </ListView>
        </VerticalStackLayout>

    </ScrollView>
</ContentPage>